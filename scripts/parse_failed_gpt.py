import logging

def parse_failed_gpt():
    """Placeholder script to parse failed GPT outputs."""
    logging.basicConfig(level=logging.INFO)
    logging.info("Parsing failed GPT outputs (stub)")

if __name__ == "__main__":
    parse_failed_gpt()
