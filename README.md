# Auto Pipeline

This repository requires environment variables for interacting with OpenAI, Notion, and Slack.

1. Copy `.env.example` to `.env`:

   ```bash
   cp .env.example .env
   ```

2. Fill in your credentials for each variable in `.env`.

The `.env` file is excluded from version control via `.gitignore`.
